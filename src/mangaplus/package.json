{
  "name": "publoader-ext-mangaplus",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "publoader extension for MangaPlus by Shueisha (extension API v2)",
  "license": "AGPL-3.0-or-later",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "esbuild src/index.ts --bundle --platform=node --format=esm --target=node20 --outfile=index.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
